# AGENTS.md · Godot 实现仓协作规则

本文件约束所有在 `shuai-heng/wukong-81-godot` 工作的人类与 AI 工程代理。

## 1. 仓库职责

本仓是 **Godot 实现仓**，负责：

- Godot 场景、脚本与项目配置；
- 正式运行时美术与数据接入；
- 自动测试、冒烟、关键帧探针；
- Godot 实现侧的验证证据与诊断工具。

产品 / 剧情 / 角色成长 / 八十一难机制的事实源在 `shuai-heng/wukong-81-trials`。若两仓描述冲突，不要在本仓自行改写产品方向；先以产品仓正式文档为准，再实现到 Godot。

## 2. 最小启动顺序

接手任务时按以下顺序恢复：

1. `README.md`
2. `AGENTS.md`
3. `docs/REPOSITORY_MAP.md`
4. `project.godot`
5. 当前任务直接相关的 `scripts/`、`data/`、`art/`、`tests/`、`evidence/`

禁止为了“理解项目”无差别遍历全部历史媒体和日志。

## 3. 根目录硬规则

根目录只允许长期入口与顶层治理文件，例如：

- `.gitignore`
- `README.md`
- `AGENTS.md`
- `project.godot`
- 一级正式目录

以下内容 **不得再新建在根目录**：

- `V5_*` / `V6_*` / `V6_1_*` 等版本截图、录像、报告；
- `ART_*` / `SKILL_*` 等导入验收产物；
- 临时日志、截图、录屏；
- 一次性 Python / PowerShell 诊断脚本；
- 本机路径说明或临时恢复文件。

正确落位：

- 当前验证证据 → `evidence/current/` 或任务对应的 `evidence/<task>/`
- 历史证据 → `evidence/archive/<milestone>/`
- 诊断工具 → `tools/diagnostics/`
- 生成 / 转换工具 → `tools/`
- 实现说明 → `docs/`

## 4. 不得随意移动的运行资产

以下目录可能被 `res://` 路径、动态加载或测试脚本引用，任何移动 / 重命名都必须先做引用扫描：

- `art/`
- `assets/`
- `data/`
- `scenes/`
- `scripts/`

移动这些目录中的文件前至少满足：

1. 搜索代码与 Godot 场景中的旧路径引用；
2. 修正所有引用；
3. 执行 Godot import / parse 检查；
4. 执行对应 smoke / probe；
5. 再提交迁移。

整理仓库不能以破坏运行时为代价。

## 5. 历史媒体迁移规则

根目录既有截图、GIF、JPG、MP4、报告属于历史证据，不直接删除。

迁移时：

- 尽量保持原始 blob，不重编码媒体；
- 按里程碑归档；
- 若 README、报告或工具引用旧路径，同提交修复；
- 大型录像若只是临时调试产物，后续优先放 Release / 外部制品，不继续长期堆在源码根目录。

## 6. 测试与证据

代码任务完成后，根据改动范围执行最小充分验证：

- Godot import / parse 无脚本错误；
- 主线 smoke；
- 涉及关键帧 / 动作系统时运行 KF probe；
- 涉及视觉编排时保留必要截图或短录像；
- 证据落入 `evidence/`，不要写到根目录。

不得用“看起来应该能跑”替代实际验证，也不得伪造未执行的测试。

## 7. 文档与状态

- `README.md` 只描述当前稳定事实与入口，不维护容易过期的长任务清单。
- 详细仓库结构放在 `docs/REPOSITORY_MAP.md`。
- 历史进度优先由 Git commit 与 `evidence/` 追溯，不在根目录复制多份状态报告。
- 新里程碑如果改变目录职责，要同步更新 `README.md` / `docs/REPOSITORY_MAP.md`。

## 8. Commit 原则

一个 commit 尽量只做一种事情：实现、测试、证据、结构治理不要无边界混杂。

仓库整理优先分成：

1. 文档 / 规则治理；
2. 历史根目录产物归档；
3. 工具脚本归位；
4. 运行资产迁移（如确有必要）。

其中第 4 类风险最高，必须独立验证。

## 9. 禁止事项

- 禁止删除尚未确认用途的素材或证据；
- 禁止因为“看起来重复”直接删 V5 / V6 / V6.1 资产；
- 禁止把产品设计重新复制一份到本仓形成第二事实源；
- 禁止把新的截图、视频、日志继续提交到根目录；
- 禁止大范围移动运行资产后不跑测试。
