# 大圣火线：八十一难 · Godot

> 本仓库是《大圣火线：八十一难》的 **Godot 运行时实现仓**。
>
> 产品方向、剧情顺序、人物外传、神通门禁与八十一难设计总纲，以 `shuai-heng/wukong-81-trials` 为产品 / 设计事实源；本仓只维护 Godot 代码、美术接入、数据、测试与运行证据，避免两套文档互相漂移。

## 当前状态

当前 `main` 已推进到 **M2 R4 / V6.1 战斗演出接入阶段**，不是早期 G00 骨架状态。

目前已包含：

- 唐僧、悟空等英雄的关键帧姿势驱动与状态过渡；
- 关键帧插值、交叉淡化、动作状态混合；
- 顿帧、震屏、拖尾、残影、发光等演出元数据；
- 身体 / 武器锚点与世界空间技能编排；
- 悟空近战武器轨迹；
- 唐僧远程咒语弹幕与独立角色技能主色；
- 章节冒烟、自定义关键帧探针与视觉证据链。

最新正式状态应以 `main` 最新 commit、`evidence/` 和测试结果为准，不再维护 README 内的手写“G00/G01/G02 待办清单”。

## 运行

建议使用 Godot 4.7.x。

```powershell
# 正常运行
godot --path .

# 无窗口逻辑冒烟
godot --headless --path . -- --smoke
```

本机 Godot 未加入 PATH 时，将 `godot` 替换为本机 Godot 可执行文件完整路径。

## 仓库地图

| 路径 | 职责 |
|---|---|
| `project.godot` | Godot 工程入口与项目配置 |
| `scenes/` | 正式场景 |
| `scripts/` | 正式运行时代码 |
| `data/` | 关键帧、锚点、章节 / 战斗数据 |
| `art/` | 正式接入的角色、Boss、小怪、姿势、技能美术 |
| `assets/` | 运行时通用资源 |
| `art_source_exact/` | 美术源文件 / 精确来源快照与校验材料 |
| `tests/` | 冒烟、关键帧探针与回归测试 |
| `tools/` | 生成、诊断、转换、验证辅助工具 |
| `evidence/` | 测试日志、截图、录像、验收与修复证据 |
| `docs/` | Godot 仓特有的结构、接入、维护说明 |

更详细的“该留 / 该迁 / 该归档”清单见 [`docs/REPOSITORY_MAP.md`](docs/REPOSITORY_MAP.md)。

## 根目录规则

从现在开始，根目录只保留 **工程入口、协作规则和顶层说明**。禁止继续把开发产物平铺到根目录。

- 截图、录像、测试输出、验收报告 → `evidence/`
- 历史版本证据 → `evidence/archive/<milestone>/`
- 诊断 / 回放 / 生成脚本 → `tools/`
- Godot 运行时正式代码 → `scripts/`
- 运行时正式数据 → `data/`
- 正式接入美术 → `art/` / `assets/`
- Godot 仓自身说明 → `docs/`

旧的 `ART_*`、`V5_*`、`V6_*`、`V6_1_*` 根目录文件暂不直接删除；它们属于历史验收材料，将在单独的“历史产物归档”提交中迁移，避免一次整理误伤 Godot 引用或验收链。

## AI / 工程协作

所有 AI 或工程代理在修改本仓前必须先读：

1. `README.md`
2. `AGENTS.md`
3. `docs/REPOSITORY_MAP.md`
4. 当前任务直接相关的代码 / 数据 / 证据

不要把历史截图、Harness 摘要或聊天自述当作当前仓库事实；以实际 `main`、正式代码、数据与测试结果为准。

## 产品设计事实源

产品主线不在本仓重复维护。以下内容统一从 `shuai-heng/wukong-81-trials` 读取：

- 唐僧启程与四章收徒顺序；
- 人物外传与神通解锁；
- 哪吒 / 杨戬等神将解锁；
- 八十一难能力门禁；
- 角色定位、剧情来源层级与产品红线。

这样做的目的只有一个：**一个仓负责“游戏应该是什么”，一个仓负责“Godot 里实际做成什么”，不再两边各写一套。**
