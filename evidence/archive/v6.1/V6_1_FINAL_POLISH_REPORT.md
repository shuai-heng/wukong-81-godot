# V6.1 FINAL POLISH 报告

时间：2026-09-12 · 范围：polish-only（Owner 授权三项），V6.1 映射/技术基座未动
执行方式：全程 Windows 隐藏桌面离屏渲染

## 1. debug_visuals 开关

- `skill_gallery_v6.gd` 新增 `debug_visuals`（默认 **关**）：`--debug-visuals` 参数或 F1 开（开发预览用）。
- 受控项：红色命中圈、无敌半透明闪、轨迹线、残影、震屏、HUD 六个状态指示灯。
- 不受影响的真实逻辑：hitbox 状态数据、VFX 强度光晕、FormOverlay、命中白闪、顿帧（40–110ms 播放冻结）全部照常。
- 最终验收视频以 debug_visuals=关 录制；脚本对 debug 开启的录制直接拒绝（防呆）。

## 2. 矩形/方形 Pose 特效边界清理

- 系统检测：288 张姿势按"不透明 bbox 边缘带填充率"扫描，>0.5 判为满幅方形特效板 → **12 张**（杨戬 32/33/35、悟空 32/35、唐僧 29/35、沙悟净 32、白龙马 35、小白龙 35、八戒 32/35）。
- 清理方式（无重绘）：bbox 内 **alpha 羽化 24px + 46px 圆角**（标准圆角矩形 SDF），只改外圈 alpha；RGB 通道全图不变；内区（去四角）alpha 逐像素不变——已程序断言 12/12 通过。
- 清理版存放 `res://art/v6_pose_clean/`（原图 `res://art/v6_pose/` 未动）；预览器优先加载清理版，Pose 映射 JSON 未改。
- 目检：before/after 对照确认方形硬边全部消除，人物本体清晰完整。

## 3. 战斗阅读尺寸（只调 Godot scale）

- 主预览基准 280px → **360px**（1280×720 画面中普通动作主体清晰可读）。
- 法相/终结技类别再 ×1.14（叠加数据自带的 scale 1.0–1.55），演出层级明显放大。
- 源 PNG 未动。

## 4. 最终验收视频

- `V6_1_FINAL_POLISH.mp4`：H.264 High / **1280×720** / 30fps / **39.87s** / 1196 帧，Movie Maker 固定时间步逐帧渲染（真连续，非时间点拼接），debug_visuals=关。
- 内容（按 Owner 指定）：悟空 法天象地+七十二变+火眼金睛+终结 → 唐僧 禅音驱邪+佛光金身 → 八戒 开山砸耙+全屏聚怪 → 哪吒 三头六臂 → 杨戬 天眼+灌江猎神终结。
- 连续性证据：0.5s 间隔采样 78 对相邻帧全部有画面变化（冻结段 0）；五帧抽检见 `V6_1_FINAL_VIDEO_EVIDENCE.jpg`。

## 5. 红线自查

- 未重做 Pose 映射（V6_1 JSON 与 288 Pose 库未动，映射 diff 为零）✓
- 未重新生成图片（清理仅为 alpha 外圈羽化，RGB/人物本体未动，程序断言通过）✓
- 未推翻 V6.1；V6/V5/V4 材料均未回滚 ✓
