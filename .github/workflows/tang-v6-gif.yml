name: Tang V6 Complete Game GIF

on:
  push:
    branches:
      - task/pixel-complete-v6-r5
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tang-gif:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout complete-game branch
        uses: actions/checkout@v4

      - name: Install ffmpeg and virtual display
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ffmpeg xvfb unzip

      - name: Download Godot 4.7.2
        run: |
          curl -L --fail --retry 3 \
            -o /tmp/godot.zip \
            https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip
          echo 'cadd3204e728a35d3f13adb7fd0d7902636b79f6b95c40c265eb73b6c35329e4  /tmp/godot.zip' | sha256sum -c -
          unzip -q /tmp/godot.zip -d /tmp/godot
          mv /tmp/godot/Godot_v4.7.2-stable_linux.x86_64 /tmp/godot/godot
          chmod +x /tmp/godot/godot
          /tmp/godot/godot --version

      - name: Import project and compile scripts
        run: |
          mkdir -p evidence/tang-v6
          /tmp/godot/godot --headless --editor --path . --quit-after 180 2>&1 | tee evidence/tang-v6/import.log
          ! grep -E 'SCRIPT ERROR|Parse Error|Failed to load script' evidence/tang-v6/import.log

      - name: Record real complete-game Tang slice
        run: |
          xvfb-run -a /tmp/godot/godot --path . -s tests/tang_v6_movie.gd --write-movie evidence/tang-v6/tang-v6-complete-game.avi 2>&1 | tee evidence/tang-v6/movie.log
          grep -q 'TANG_V6_MOVIE_DONE' evidence/tang-v6/movie.log
          test -s evidence/tang-v6/tang-v6-complete-game.avi
          ! grep -E 'SCRIPT ERROR|Parse Error|Invalid get index|Invalid access' evidence/tang-v6/movie.log

      - name: Convert to quick GIF and MP4
        run: |
          ffmpeg -y -i evidence/tang-v6/tang-v6-complete-game.avi -vf "fps=16,scale=768:-1:flags=lanczos,split[s0][s1];[s0]palettegen=max_colors=160[p];[s1][p]paletteuse=dither=bayer:bayer_scale=3" evidence/tang-v6/tang-v6-preview.gif
          ffmpeg -y -i evidence/tang-v6/tang-v6-complete-game.avi -c:v libx264 -preset veryfast -crf 22 -pix_fmt yuv420p evidence/tang-v6/tang-v6-preview.mp4
          ls -lh evidence/tang-v6/

      - name: Upload Tang preview
        uses: actions/upload-artifact@v4
        with:
          name: tang-v6-complete-game-preview
          path: |
            evidence/tang-v6/tang-v6-preview.gif
            evidence/tang-v6/tang-v6-preview.mp4
            evidence/tang-v6/movie.log
            evidence/tang-v6/import.log
          retention-days: 7
