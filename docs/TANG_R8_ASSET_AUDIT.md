# 唐僧 R8 释放关键帧素材复核

对原 V6 `POSE_24` 重新做逐像素视觉复核后，确认动作本身（右掌前推、左手持杖、重心向前）适合做唐僧远程释放帧，但原图右侧仍含巨大金色月牙，不能作为人物层直接使用。

R8 要求：

- `art/v7_clean/tang_sanzang/tang_sanzang__POSE_24__CAST_CLEAN.png` 必须只保留唐僧人物、锡杖、前推右掌与衣物；
- 不允许右侧大月牙、底部大范围烟云、莲瓣、大法阵进入人物 PNG；
- Release 时人物层只显示清理后的角色，法印、弹体、Impact 全部由独立世界对象/程序 VFX 生成；
- 若清理资产缺失，运行时只能回退到干净合掌帧，绝不能回退原始 POSE24；
- 后续 GIF/录像验收必须来自 Godot 实机，不再使用手工拼接 storyboard 作为通过证据。
